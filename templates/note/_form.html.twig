
{{ form_start(form) }}
                {{ form_row(form.title, {'attr': {
                    'class': 'mb-3 form-control'
                }}) }}
                {{ form_row(form.description, {'attr': {
                    'class': 'mb-3 form-control',
                    'style': 'resize: none'
                }}) }}

<div class="mb-3">
    {{ form_row(form.tags, {'attr': {
        'class': 'me-1',
        'multiple': 'multiple',
        'data-elements': tags|json_encode
    }}) }}
</div>
<div>
    {{ form_widget(form.publish, {'attr': {
        'class': 'me-1',
    }}) }}
    <label for="note_publish" class="pointer">Es Público</label>
</div>

<div class="d-flex justify-content-end">
    <button class="btn btn-success me-2"><i class="fa fa-archive me-2"></i>Guardar</button>
    <a href="{{ path('app_note_index') }}" class="btn btn-danger"><i class="fa fa-times"></i> Cancelar</a>
</div>
{{ form_end(form) }}
